/**
 * Contact addresses for The Standing Chair.
 *
 * SUPPORT_EMAIL is the human-facing inbox: it is shown in the footer, the
 * account menu, and on public shop pages, and it is used as Reply-To on every
 * outgoing client email so replies land in a real mailbox.
 *
 * SENDER_LABEL is the friendly From name used on outgoing email.
 */
export const SUPPORT_EMAIL = "support@thestandingchair.com";
export const SUPPORT_MAILTO = `mailto:${SUPPORT_EMAIL}`;
export const SENDER_LABEL = "The Standing Chair";
